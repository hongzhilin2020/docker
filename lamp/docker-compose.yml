version: "3"
services:

   web_server:
     build:
       context: https://github.com/qiubin8888/docker.git#:lamp
       container_name: lamp
       volumes:
         - ${PWD}:/var/lib/mysql
       restart: always